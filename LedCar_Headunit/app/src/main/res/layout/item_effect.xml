<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/effectItemRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="10dp"
    android:paddingVertical="8dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <!-- Static color swatch - see EffectAdapter#colorsForName. The real
         animated reproduction of whichever effect is selected lives on the
         ambient preview's neon-strip mask instead (see
         AmbientLightingController.previewNeonStripEffect), not duplicated
         here in the scrolling list. -->
    <View
        android:id="@+id/effectPreview"
        android:layout_width="26dp"
        android:layout_height="15dp" />

    <TextView
        android:id="@+id/effectName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:textColor="@color/text_primary"
        android:textSize="13sp" />

    <View
        android:id="@+id/effectSelectedDot"
        android:layout_width="7dp"
        android:layout_height="7dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/dot_selected"
        android:visibility="invisible" />
</LinearLayout>
